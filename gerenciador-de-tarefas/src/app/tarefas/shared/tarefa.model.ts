export class Terefa {
    
    constructor(
        //?: - utilizando o operador ? os valores são opcional 
        public id?: number,
        public nome?: string,
        public concluida?: boolean){}
}
